import { fetchID, fetchConfig } from '../fetch'

const Card = (props) => {
    const { id, option } = props;
    //image
    //title
    //date
    //logos
    // pass on -> image, title, date, runtime, airdate, episodes, seasons, overvoew, streaminging where (tv)
    return (
        <div>
        </div>
    )
}

export default Card;